<?xml version="1.0"?>
<ruleset name="LDAP User Manager">
    <description>PSR-12 coding standards for LDAP User Manager</description>
    
    <!-- Include PSR-12 ruleset -->
    <rule ref="PSR12"/>
    
    <!-- Exclude vendor and build directories -->
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>node_modules/*</exclude-pattern>
    <exclude-pattern>build/*</exclude-pattern>
    <exclude-pattern>dist/*</exclude-pattern>
    
    <!-- Exclude JavaScript and CSS files -->
    <exclude-pattern>www/js/*.js</exclude-pattern>
    <exclude-pattern>www/bootstrap/*</exclude-pattern>
    <exclude-pattern>www/request_account/fonts/*</exclude-pattern>
    
    <!-- Exclude Docker and configuration files -->
    <exclude-pattern>*.yml</exclude-pattern>
    <exclude-pattern>*.yaml</exclude-pattern>
    <exclude-pattern>*.toml</exclude-pattern>
    <exclude-pattern>Dockerfile</exclude-pattern>
    <exclude-pattern>entrypoint</exclude-pattern>
    
    <!-- Include PHP files -->
    <file>www/</file>
    
    <!-- Show progress -->
    <arg value="p"/>
    
    <!-- Show colors -->
    <arg value="s"/>
    
    <!-- Show source codes -->
    <arg value="n"/>
    
    <!-- Show sniff codes -->
    <arg value="a"/>
</ruleset>
