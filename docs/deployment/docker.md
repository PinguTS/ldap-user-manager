# Docker Deployment

The full Docker deployment guide is at [DOCKER-SETUP.md](../../DOCKER-SETUP.md) in the repository root.

It covers:

- Architecture overview (Caddy, Dex, LDAP, LDAP User Manager)
- Prerequisites
- Quick start with `setup-oidc.sh`
- Step-by-step Portainer stack setup (LDAP server stack + User Manager stack)
- Web-based setup wizard
- Verification and testing commands
- Troubleshooting common Docker issues
- Security considerations

For configuring environment variables after the containers are running, see [Environment Variables](../configuration/environment-variables.md).
