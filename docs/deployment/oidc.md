# OIDC Integration

The full OpenID Connect (OIDC) integration guide is at [identity.md](../identity.md).

It covers:

- Architecture overview: Dex as OIDC provider, LDAP User Manager, and external services
- OIDC authentication flow (sequence diagrams)
- Environment variables for OIDC configuration
- DNS and certificate setup for production
- External service integration (TYPO3, GitLab, Nextcloud)
- Adding new OIDC clients to Dex
- Security checklist
- Troubleshooting common OIDC errors

For per-service configuration guides, see the [services/](../../services/) directory.
