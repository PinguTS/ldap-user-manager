function generatePassword(numberOfWords,seperator,passwordField,confirmField){if(typeof numberOfWords==="undefined"){numberOfWords=4}if(typeof seperator==="undefined"){seperator=" "}if(typeof passwordField==="undefined"){passwordField="password"}if(typeof confirmField==="undefined"){confirmField="confirm_password"}var words=[];for(var i=0;i<numberOfWords;i++){var randomIndex=Math.floor(Math.random()*wordlist.length);words.push(wordlist[randomIndex])}var password=words.join(seperator);document.getElementById(passwordField).value=password;if(confirmField&&document.getElementById(confirmField)){document.getElementById(confirmField).value=password}return password}
